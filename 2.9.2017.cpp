/*
void feetInches::simplify() {
	if (inches >= 12) {
		feet += inches / 12;
		inches %= 12;
	}
}

// what if i want to add an integer to a feetinches object
// adding a feetinches object and an int togeter

int x = 42;
f1 = f3 + x;	// f1, f3 are feetInches objects
// This doesnt create an error
// The compiler goes into constructor makes feetInches object with 42 feet as the parameter44

friend feetInches operator+(const feetInches& left, const feetInches& right);
// This should be used with all function so that you can use integers on the left and the right

//+= operator returns a modified feetInches operator

namespace cs_feetInches {
	class blah
		blah
		blah;
}

namespace cs_feetInches {
	Class definitions
}
*/